import 'dart:io';

void main() {
  // stdout.write("Enter the number = ");
  // int a = int.parse(stdin.readLineSync()!);

  // stdout.write("Enter the number = ");
  // int b = int.parse(stdin.readLineSync()!);

  // stdout.write("The greater number is $a > $b");

  int a = 50;
  int b = 40;
  if (a == b) {
    print("$a == $b");
  } else if (a != b) {
    print("$a != $b");
  } else {
    print(" $a > $b");
  }
}
